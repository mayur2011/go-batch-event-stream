# go-batch-event-stream
Batch Event Stream is a generic solution written in Go to allow applications to process historical data as well as real-time data as event streaming with configurable time delay.
